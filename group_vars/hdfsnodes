---
# file: group_vars/hdfsnodes

# The name of the default file system. A URI whose scheme and authority determine the FileSystem implementation.
fs.default.name :

# The secondary namenode http server address and port.
dfs.namenode.secondary.http-address : ip-172-31-28-165.eu-west-1.compute.internal

# The actual number of replications can be specified when the file is created.
dfs.replication : 1

# Determines where on the local filesystem the DFS name node should store the name table(fsimage). If this is a comma-delimited list of directories then the name table is replicated in all of the directories, for redundancy.
dfs.name.dir : /home/ubuntu/data/nn

# Determines where on the local filesystem the DFS name node should store the name table(fsimage). If this is a comma-delimited list of directories then the name table is replicated in all of the directories, for redundancy.
dfs.data.dir : /home/ubuntu/data/dn

# Determines where on the local filesystem the DFS secondary name node should store the temporary images to merge. If this is a comma-delimited list of directories then the image is replicated in all of the directories for redundancy.
dfs.namenode.checkpoint.dir : file:///home/ubuntu/data/sn


# The java implementation to use.  Required.
java_home : /opt/java/64/jdk1.6.0_43/

# The maximum amount of heap to use, in MB. Default is 1000.
hadoop_heapsize : 2000

namenode.sink.ganglia.servers : :8649

datanode.sink.ganglia.servers : :8649
